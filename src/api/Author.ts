export default class Author {
    name: string;
    avatar: string;
    link: string;

    constructor(name: string, avatar: string, link: string) {
        this.name = name;
        this.avatar = avatar;
        this.link = link;
    }
}